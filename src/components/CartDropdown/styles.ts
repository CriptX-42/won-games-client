import styled from 'styled-components'

import { Wrapper as CartWrapperStyles } from 'components/CardList/styles'

export const Wrapper = styled.main`
  ${CartWrapperStyles} {
    width: 56rem;
  }
`
